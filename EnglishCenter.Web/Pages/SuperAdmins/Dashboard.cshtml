@page
@model EnglishCenter.Web.Pages.SuperAdmins.DashboardModel
@{
    Layout = "~/Pages/Shared/_SuperAdminLayout.cshtml";
    ViewData["Title"] = "Dashboard";
}

<div class="page-header">
    <div>
        <h2 class="page-title">Dashboard</h2>
        <p class="page-subtitle">Toàn cảnh hệ thống và hiệu suất tổng thể.</p>
    </div>
    <div>
        <button class="btn btn-primary">Export Report</button>
    </div>
</div>

<div class="row g-4 mb-4">
    <div class="col-sm-6 col-xl-3">
        <div class="card sa-card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Total Users</div>
                <div class="stat-value">@Model.TotalUsers</div>
                <div class="stat-change neutral">Live from API</div>
            </div>
        </div>
    </div>

    <div class="col-sm-6 col-xl-3">
        <div class="card sa-card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Campuses</div>
                <div class="stat-value">@Model.TotalCampuses</div>
                <div class="stat-change neutral">Live from API</div>
            </div>
        </div>
    </div>

    <div class="col-sm-6 col-xl-3">
        <div class="card sa-card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Students</div>
                <div class="stat-value">@Model.TotalStudents</div>
                <div class="stat-change neutral">Live from API</div>
            </div>
        </div>
    </div>

    <div class="col-sm-6 col-xl-3">
        <div class="card sa-card stat-card h-100">
            <div class="card-body">
                <div class="stat-label">Monthly Revenue</div>
                <div class="stat-value">@Model.TotalCollectedRevenue.ToString("N0")</div>
                <div class="stat-change neutral">Collected revenue</div>
            </div>
        </div>
    </div>
</div>

<div class="row g-4 mb-4">
    <div class="col-lg-8">
        <div class="card sa-card h-100">
            <div class="card-header card-header-clean">
                <h5 class="mb-0">Revenue Overview</h5>
            </div>
            <div class="card-body">
                <div class="chart-placeholder">
                    Revenue chart area
                </div>
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card sa-card h-100">
            <div class="card-header card-header-clean">
                <h5 class="mb-0">System Alerts</h5>
            </div>
            <div class="card-body">
                <ul class="list-group list-group-flush sa-list">
                    <li class="list-group-item">
                        <div class="fw-semibold">Failed login spike</div>
                        <small class="text-muted">Detected on Campus A within the last hour.</small>
                    </li>
                    <li class="list-group-item">
                        <div class="fw-semibold">Permission override used</div>
                        <small class="text-muted">A sensitive action was overridden by SuperAdmin.</small>
                    </li>
                    <li class="list-group-item">
                        <div class="fw-semibold">Finance sync delayed</div>
                        <small class="text-muted">Global revenue sync is running slower than expected.</small>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>

<div class="row g-4">
    <div class="col-lg-7">
        <div class="card sa-card h-100">
            <div class="card-header card-header-clean">
                <h5 class="mb-0">Recent Activities</h5>
            </div>
            <div class="card-body">
                @foreach (var log in Model.RecentActivities)
                {
                    <div class="timeline-item">
                        <div class="timeline-title">@log.Action</div>
                        <div class="timeline-text">@log.EntityName</div>
                        <div class="timeline-time">@log.CreatedAt.ToString("dd/MM/yyyy HH:mm")</div>
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="col-lg-5">
        <div class="card sa-card h-100">
            <div class="card-header card-header-clean">
                <h5 class="mb-0">Top Campuses</h5>
            </div>
            <div class="card-body">
                @foreach (var campus in Model.TopCampuses)
                {
                    <div class="campus-row">
                        <div>
                            <div class="fw-semibold">@campus.CampusName</div>
                            <small class="text-muted">@campus.CampusCode</small>
                        </div>
                        <div class="text-end">
                            <div class="fw-bold">@campus.CollectedRevenue.ToString("N0")</div>
                            <small class="text-muted">Invoices: @campus.InvoiceCount</small>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
